import { AuthProvider } from './AuthProvider';
import './App.css';
import Router from './routes/Router';

function App() {
  return (
    // <AuthProvider>
      <Router/>
    // </AuthProvider>
  );
}

export default App;
